
<!-- 
    MAKE A CARD FOR EVERY SHOP
    USE BOOTSTRAP
    First render all the shops, we will later add in the filtering logic
-->
    <%- include("partials/header.ejs") %> 
<style>
    /* STYLING HERE */
    .card {
        background-color: #fff;
        border: 1px solid rgba(0, 0, 0, 0.125);
        border-radius: 0.25rem;
        box-shadow: rgba(0, 0, 0, 0.35) 0px 5px 15px;
    }
    .card-title, .card-subtitle {
        text-align: center;
    }
    .form-control {
        margin: 10px auto;
    }
</style>

<!-- MAKE A SEPERATE ONE. DON'T CHANGE ANYTHING VARCHA -->
<!-- 
    http://localhost:3000/customer/list
 -->
<div class="py-5">
    <div class="container">
        <div class="row">
            <% for(let i=0; i<shops.length; i++){ %>
                <div class="col-4">
                    <div class="card">
                        <div class="card-body">
                            <h6 class="card-title text-muted">Business Name</h6>
                            <h5 class="card-subtitle mb-2"><%= shops[i].businessName %></h5>
                            <h6 class="card-title text-muted">Domain Name</h6>
                            <h5 class="card-subtitle mb-2"><%= shops[i].domains %></h5>
                            <form action="/business/<%= shops[i].id %>/enquire" method="POST" class="form-group">
                                <input 
                                    type="text" 
                                    placeholder="Make an enquiry regarding the product you need" 
                                    name="enquiry"
                                    class="form-control"
                                />
                                <button class="btn btn-block btn-primary">Enquire!</button>
                            </form>
                        </div>
                    </div>
                </div>
            <% } %>
        </div>
    </div>
</div>